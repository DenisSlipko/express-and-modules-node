export const MONGODB_URI: string = (process.env.MONGODB_URI as string)
export const PASSWORD: string = (process.env.MONGODB_PASSWORD as string)
export const PORT: string = (process.env.PORT as string)
